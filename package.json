{
  "name": "@kotoba-lang/quarantine",
  "version": "0.1.0",
  "private": true,
  "description": "Reversible removal — move to a vault, restore exactly, purge only under a retention floor.",
  "type": "module",
  "scripts": {
    "test": "kbb --backend sci --classpath src:test -m quarantine.test-runner"
  }
}
